
# type X = (x)
# var xs = [X 1, X 2, X 3]
# xs

# type XY = (x, y)
# var xys = [(XY 1, 2), (XY 3, 4), (XY 5, 6)]
# xys


def abs(n: Num) -> Num =
  if n < 0
    -n
  else
    n

abs 1
abs -1


def min(a: Num, b: Num) -> Num =
  if a < b
    a
  else
    b

min  1, 2
min -1, 2
